---
title: "性能"
---

# 性能

存储性能不是单一指标，需要同时看 IOPS、吞吐、延迟、元数据操作和应用访问模式。

## 优化建议

- CBS：按数据库写入模式选择云硬盘类型，关注随机写、fsync 和日志盘。
- CFS：减少单目录海量小文件，避免多个副本频繁写同一文件。
- COS：适合大文件和对象访问，不适合高频 rename/list/stat。
- AI 数据集：优先设计预热、缓存和分片，避免训练启动时所有 Pod 同时拉取。

## 验证方式

上线前用业务真实读写模式压测。简单 `dd` 只能粗略检查吞吐，不能代表数据库、训练任务或小文件场景。
